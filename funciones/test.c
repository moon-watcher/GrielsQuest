#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

volatile int temp;
volatile long val_l;
char *ptr;

int i;
long l;
volatile int ret;
char *str;
char *fmt;

int main (void)
{
//	printf ("toto");

	printf ("tutu %c\n", 0x24);

	/*temp = abs (1200);
	temp = abs (-1200);

	val_l = strtol ("   0x2Abcagaga",	&ptr, 16);*/
	
/*	if (isalpha('A'))
	{
		str = "   -235, mon cul, ";
		fmt = "%i  %D";

		ret = sscanf ((const char*) str, (const char*) fmt, &i, &l);

		i=i+1;
		l=l+1;
	} 
	else
	{
		for (i=0; i<100; i++)
		{
			l= l+1;
		}
	}	 */


	for (;;);
}
